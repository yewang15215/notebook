# MySQL 语法

* 注释
    - `# `
    - `-- `
    - `/* */`
    - 注意
        ```
        /*!50001 DROP TABLE IF EXISTS `count_yysbh`*/; 
        ```
        > 表示假如 数据库版本是5.00.01以上版本，“DROP TABLE IF EXISTS `count_yysbh”才会被执行。
